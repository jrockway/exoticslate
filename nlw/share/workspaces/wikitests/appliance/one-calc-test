if [ "$1" == "" ]; then
    echo USAGE: source one-calc-test "TESTCASE"  
else
    export WIKIEMAIL=$HOSTNAME
    USERNAME="wikitester@ken.socialtext.net"
    alias tail='tail'
    sudo -u www-data  run-wiki-tests --no-maximize --test-username $USERNAME --test-email $USERNAME  --plan-server http://$HOSTNAME  --plan-workspace calctests --test-server http://$HOSTNAME  --timeout 60000 --plan-page "$1"   
fi
