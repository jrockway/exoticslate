{var lastIndex = matches.length-1}
{for t in matches}
 &lt;a href="javascript:addTag('${t.tag|escapespecial}')" title="Add ${t.tag} to page" class="st-tags-suggestion"&gt;${t.tag}&lt;/a&gt;{if t_index != lastIndex} : {/if}
{/for}
