&lt;span class="st-attachmentsqueue-listlabel">Files To upload: &lt;/span>
{var lastIndex = queue.length-1}
{for file in queue}
 &lt;span class="st-attachmentsqueue-filelist-name">${file}&nbsp;</span>
 &lt;a href="#" onclick="javascript:window.EditQueue.remove_index(${file_index}); return false" title="Remove ${file} from the queue" class="st-attachmentsqueue-filelist-delete">[x]&lt;/a>
{if file_index != lastIndex}, {/if}
{/for}
